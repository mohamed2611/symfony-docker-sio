{% extends 'base.html.twig' %}

{% block title %}Modif FollowUp{% endblock %}

{% block body %}
    <h1>Modifier le suivi
    {{ follow_up.id }}</h1>

    {{ include('follow_up/_form.html.twig', {'button_label': 'Update'}) }}

  <a href="{{ path('app_follow_up_index') }}">
        <i class="bi bi-chevron-left"></i> &nbsp;Retour à la liste
    </a>

    {{ include('follow_up/_delete_form.html.twig') }}
{% endblock %}
