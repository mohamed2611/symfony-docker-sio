{% extends 'base.html.twig' %}

{% block title %}FollowUp{% endblock %}

{% block body %}
    <h1>Fiche de suivi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ follow_up.id }}</td>
            </tr>

             <tr>
                <th>Vétérinaire</th>
                <td>{{ follow_up.getVeterinary.getName }}</td>
            </tr>

            <tr>
                <th>Contact</th>
                <td>{{ follow_up.contactName }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ follow_up.comment }}</td>
            </tr>
            <tr>
                <th>Date d'appel    </th>
                <td>{{ follow_up.callDate ? follow_up.callDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

  <a href="{{ path('app_follow_up_index') }}">
        <i class="bi bi-chevron-left"></i> &nbsp;Retour à la liste
    </a>

    <a href="{{ path('app_follow_up_edit', {'id': follow_up.id}) }}">editer</a>

    {{ include('follow_up/_delete_form.html.twig') }}
{% endblock %}
